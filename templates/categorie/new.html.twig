{% extends 'commun/base.html.twig' %}

{% block title %}{{ "Nouvelle catégorie"|trans }}{% endblock %}

{% block body %}

<main role="main" class="container-fluid">
    <div class="row">
        <div class="col-md-10">
            <div class="main-div">
                <div class="card col-md-12">
                    <div class="card-header">
                        <h3>{{ "Créer une catégorie"|trans }}</h3>
                    </div>
                    <div class="card-body">
                        {{ include('categorie/_form.html.twig') }}

                        <a href="{{ path('categorie_index') }}">Retour à la liste des catégories</a>

                    </div>
                </div>
            </div>
        </div>
        {% endblock %}
