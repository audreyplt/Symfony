{% extends 'commun/base.html.twig' %}

{% block title %}{{ "Modifier catégorie"|trans }}{% endblock %}

{% block body %}
<main role="main" class="container-fluid">
    <div class="row">
        <div class="col-md-10">
            <div class="main-div">
                <div class="card col-md-12">
                    <div class="card-header">
                        <h3>{{ "Modifier une catégorie"|trans }}</h3>
                    </div>
                    {{ include('categorie/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('categorie_index') }}">retour à la liste des catégories</a>

                    {{ include('categorie/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
        {% endblock %}
