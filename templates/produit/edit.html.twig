{% extends 'commun/base.html.twig' %}

{% block title %}{{ "Modifier produit"|trans }}{% endblock %}

{% block body %}
<main role="main" class="container-fluid">
    <div class="row">
        <div class="col-md-10">
            <div class="main-div">
                <div class="card col-md-12">
                    <div class="card-header">
                        <h3>{{ "Modifier un nouveau produit"|trans }}</h3>
                    </div>

                    {{ include('produit/_form.html.twig', {'button_label': 'Valider'}) }}

                    <a href="{{ path('produit_index') }}">{{ "retour à la liste des produits"|trans }}</a>

                    {{ include('produit/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
        {% endblock %}
